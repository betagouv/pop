import { MultiList, Mapping } from "./";

describe("MultiList", () => {
  it("is truthy", () => {
    expect(MultiList).toBeTruthy();
  });
});

describe("Mapping", () => {
  it("is truthy", () => {
    expect(Mapping).toBeTruthy();
  });
});
