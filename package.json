{
	"name": "pop",
	"version": "0.0.0",
	"scripts": {
		"dev:api": "pnpm --filter pop-api dev",
		"dev:edition": "pnpm --filter pop-production run dev",
		"dev:diffusion": "pnpm --filter pop-next run dev"
	},
	"devDependencies": {
		"@biomejs/biome": "1.6.4"
	}
}
